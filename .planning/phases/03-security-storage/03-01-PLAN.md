# Plan: 03-01 — AES-256-GCM Encryption

## Goal
Implement authenticated encryption for session data to ensure PII mappings are secure at rest.

## Must-Haves
- `src/vault/encryption.ts` with `encrypt` and `decrypt` functions.
- AES-256-GCM usage with IV and Auth Tags.
- PBKDF2 key derivation from passphrase.
- Unit tests in `tests/security.test.ts`.

## Task Specs
- [ ] Implement `deriveKey` using PBKDF2.
- [ ] Implement `encrypt` with random IV and GCM tag.
- [ ] Implement `decrypt` with tag verification.
- [ ] Write unit tests for encryption/decryption cycles.

---
*Generated: 2026-04-14*
