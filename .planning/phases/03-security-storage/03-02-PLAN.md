# Plan: 03-02 — Persistent Vault Storage

## Goal
Implement file-based persistence for the `TokenVault` to support session recovery and long-running workflows.

## Must-Haves
- `save(path, passphrase)` and `load(path, passphrase)` methods in `TokenVault`.
- Vault data must be encrypted before writing to disk.
- Error handling for missing files or invalid passwords.

## Task Specs
- [ ] Update `TokenVault` with `save` and `load` logic.
- [ ] Integrate with the `encryption` module.
- [ ] Write integration tests for disk persistence.
- [ ] Verify encryption on disk (content should be unreadable).

---
*Generated: 2026-04-14*
