# Plan: 04-01 — NLP Entity Detection

## Goal
Add NLP-based entity detection to catch Names and Organizations that cannot be identified by regex alone.

## Must-Haves
- `src/detection/nlp.ts` implementing `extractEntities`.
- Support for `PERSON`, `ORG`, and `LOCATION` types.
- Zero-external-dependency execution using `compromise`.
- Unit tests in `tests/intelligence.test.ts`.

## Task Specs
- [ ] Implement `extractEntities` using `compromise`.
- [ ] Add basic deduplication and punctuation cleaning.
- [ ] Write tests for names, organizations, and combined strings.
- [ ] Integrate into the main detection pipeline (verified in Phase 2 Gateway).

---
*Generated: 2026-04-14*
