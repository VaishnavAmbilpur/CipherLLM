# Plan: 01-02 — TokenVault Mapping Logic

## Goal
Implement the `TokenVault` class which handles the mapping between original sensitive values and their anonymized tokens.

## Must-Haves
- `src/vault/vault.ts` containing the `TokenVault` class.
- **Idempotency:** The `tokenize(value, type)` method must return the same token if the same value is encountered again.
- **Token Format:** `[TYPE_N]` where N is a counter per type.
- Unit tests in `tests/vault.test.ts` verifying mapping logic and counters.

## User Context
- The vault should be in-memory for Phase 1.
- Provide a `getAll()` method to retrieve the full mapping for re-hydration.
- Provide a `destroy()` method to clear the vault.

## Task Specs
- [ ] Create `src/vault/vault.ts`.
- [ ] Implement the `TokenVault` class with a `Map` for storage and a `counters` object.
- [ ] Implement `tokenize(value: string, type: string): string`.
- [ ] Create `tests/vault.test.ts`.
- [ ] Write tests to verify:
    - Unique tokens for different values.
    - Same token for the same value.
    - Counter incrementing per type (e.g., PERSON_1, PERSON_2).
- [ ] Verify all tests pass.

---
*Generated: 2026-04-14*
