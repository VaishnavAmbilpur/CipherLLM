# Plan: 01-01 — PII Detection Regex Library

## Goal
Implement a robust collection of regex patterns for identifying sensitive data, prioritized for Indian context (Aadhaar, PAN, UPI, ₹ Salary) and universal PII (Email, Phone, CC, API Keys).

## Must-Haves
- `src/detection/regex.ts` containing the `PII_PATTERNS` constant.
- Support for Aadhaar (with/without spaces).
- Support for PAN card (5 letters, 4 numbers, 1 letter).
- Support for Indian Phone numbers (+91 or 0 prefix).
- Support for Email and generic API keys.
- Unit tests in `tests/detection.test.ts` verifying all patterns.

## User Context
- Use the patterns provided in `04_EXECUTION.md` as the baseline.
- Ensure case-insensitivity where appropriate (e.g., PAN).

## Task Specs
- [ ] Create `src/detection/regex.ts`.
- [ ] Implement `AADHAAR`, `PAN`, `PHONE_IN`, `UPI`, `SALARY_IN`, `EMAIL`, `API_KEY` patterns.
- [ ] Create `tests/detection.test.ts`.
- [ ] Write test cases for each PII type with valid and invalid samples.
- [ ] Verify all tests pass with `npm test`.

---
*Generated: 2026-04-14*
