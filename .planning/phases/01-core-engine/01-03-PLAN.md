# Plan: 01-03 — Re-hydration Engine

## Goal
Implement the logic to restore original PII values into the LLM's anonymized response.

## Must-Haves
- `src/rehydration/rehydrate.ts` containing the `rehydrate(response, vault)` function.
- **Fuzzy Matching:** Use case-insensitive regex to find tokens in the response.
- Handle LLM-mangled tokens (e.g., `[PERSON_1]` or `[Person_1]`).
- Unit tests in `tests/rehydrate.test.ts`.

## User Context
- The re-hydration logic must iterate through the entire vault and replace every occurrence of every token found.

## Task Specs
- [ ] Create `src/rehydration/rehydrate.ts`.
- [ ] Implement `rehydrate(response: string, vault: Map<string, string>): string`.
- [ ] Use `new RegExp(token.replace(/[[\]]/g, '\\$&'), 'gi')` for replacement to ensure robustness.
- [ ] Create `tests/rehydrate.test.ts`.
- [ ] Write tests to verify:
    - Multiple tokens replacement in a single string.
    - Case-insensitive token matching.
    - No changes if no tokens are present.
- [ ] Verify all tests pass.

---
*Generated: 2026-04-14*
