# Plan: 02-01 — LLM Provider Implementation

## Goal
Create a unified abstraction for multiple LLM providers and implement support for OpenAI and Anthropic.

## Must-Haves
- `src/providers/types.ts` defining the `LLMProvider` interface.
- `src/providers/openai.ts` implementing `OpenAIProvider`.
- `src/providers/anthropic.ts` implementing `AnthropicProvider`.
- Environment variable support for API keys.

## Task Specs
- [ ] Define `LLMProvider` interface with `send(prompt: string): Promise<string>`.
- [ ] Implement `OpenAIProvider` using the `openai` npm package and `gpt-4o`.
- [ ] Implement `AnthropicProvider` using the `@anthropic-ai/sdk` and `claude-3-5-sonnet-20240620`.
- [ ] Write integration tests in `tests/providers.test.ts` (using mocks or real keys if provided).

---
*Generated: 2026-04-14*
