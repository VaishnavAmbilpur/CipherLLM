# Plan: 02-02 — Express Proxy Gateway

## Goal
Build the REST API that orchestrates the detection, tokenization, LLM call, and re-hydration.

## Must-Haves
- `src/index.ts` (or `src/server.ts`) setting up Express.
- `POST /v1/chat` endpoint.
- Session management linking each request to a `TokenVault`.
- Error handling for LLM timeouts or failures.

## Task Specs
- [ ] Initialize Express server for development.
- [ ] Implement the `POST /v1/chat` route handler.
- [ ] Implement the orchestration logic:
    1. Detect PII in prompt.
    2. Tokenize PII and store in `TokenVault`.
    3. Send sanitized prompt to LLM.
    4. Re-hydrate the LLM response.
    5. Return JSON: `{ response: string, redactionCount: number }`.
- [ ] Write end-to-end tests in `tests/gateway.test.ts` using `supertest`.

---
*Generated: 2026-04-14*
