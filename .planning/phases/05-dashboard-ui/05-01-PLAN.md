# Plan: 05-01 — UI Shell and Design System

## Goal
Set up the fundamental UI shell, typography, and color system according to the design spec.

## Must-Haves
- Dark (near black) background `#0A0A0F`.
- Typography: Space Mono, Syne, DM Sans, JetBrains Mono.
- Layout: Sidebar + Main Content (Chat + Vault).
- CSS Variables for all design tokens.

## Task Specs
- [ ] Configure `tailwind.config.ts` with the CipherLLM color palette.
- [ ] Set up global fonts in `layout.tsx` using Google Fonts.
- [ ] Define CSS variables in `globals.css`.
- [ ] Create the `Sidebar` component with the hero counter placement.
- [ ] Create the `MainShell` layout with responsive zones.

---
*Generated: 2026-04-14*
